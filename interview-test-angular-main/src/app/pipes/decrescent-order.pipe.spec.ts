import { DecrescentOrderPipe } from './decrescent-order.pipe';

describe('DecrescentOrderPipe', () => {
  it('create an instance', () => {
    const pipe = new DecrescentOrderPipe();
    expect(pipe).toBeTruthy();
  });
});
